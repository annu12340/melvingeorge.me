---
title: How to simulate a user clicking forward and back buttons in the browser using JavaScript?
description: Learn the usage of forward() and back() method in the History API using JavaScript
keywords: simulate a user clicking back button javascript,simulate user clicking forward button javascript,forward method history javascript,back method history javascript,javascript
category: JavaScript
author: MELVIN GEORGE
date: 2020-09-09
---

To simulate a user clicking forward or back button in the navigation bar in the browser, We can use the
`forward()` or `back()` methods available in the `History` API.

## Simulating a forward click

To simulate a forward click we can use the `forward()` method like this,

```javascript
// Simulate forward click
window.history.forward();
```

## Simulating a back click

To simualte a back click we can use the `back()` method like this,

```javascript
// Simulate back click
window.history.back();
```

### Feel free to share if you found this useful 😃.

---
